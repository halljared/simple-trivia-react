import '@testing-library/jest-dom';
// Optional: You can add other global setup here if needed in the future.
// For example, cleaning up after each test (though RTL's render often handles this for its scope)
// import { afterEach } from 'vitest';
// import { cleanup } from '@testing-library/react';

// // Cleans up the DOM after each test automatically
// afterEach(() => {
//   cleanup();
// });
